{
  "name": "codegen-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"python backend/app.py\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
